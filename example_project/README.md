# example_project

Smoke app for the local `hawk_gleam` catcher (`path = ".."`). Not published to Hex.

`.env` is gitignored. Copy the template, paste the Hawk integration token, then run:

```sh
cp .env.example .env
gleam run
```
